{% comment %}
  All-products: vertical editorial list on desktop (image left, copy right, page scroll),
  same split as Info/Studio. Mobile: image above copy, page scroll on #MainContent.
{% endcomment %}

<section class="all-products" data-section-id="{{ section.id }}">
  <div class="all-products__frame" id="all-products-frame">
    <div class="all-products__track" id="all-products-track">

      {%- for product in collection.products -%}
        {%- assign product_is_first = forloop.first -%}
        {%- comment -%}
          Cycle decorative frames in order. List every frame-*.png in /assets (pipe-separated).
          Product n uses frame_assets[n % count] — repeats when there are more products than frames.
        {%- endcomment -%}
        {%- assign frame_assets = 'frame-1.png|frame-2.png|frame-3.png|frame-4.png|frame-5.png' | split: '|' -%}
        {%- assign frame_idx = forloop.index0 | modulo: frame_assets.size -%}
        {%- assign frame_asset = frame_assets[frame_idx] -%}
        <div class="all-products__card" draggable="false" data-product-id="{{ product.id }}">
          
          <div class="all-products__img-wrapper">
            {%- for image in product.images -%}
              <img
                class="all-products__img{% if forloop.first %} is-active{% endif %}"
                src="{{ image | image_url: width: 800 }}"
                alt="{{ image.alt | default: product.title | escape }}"
                loading="{% if forloop.first %}eager{% else %}lazy{% endif %}"
              >
            {%- else -%}
              <div class="all-products__placeholder"></div>
            {%- endfor -%}

            <img
              class="all-products__frame-img"
              src="{{ frame_asset | asset_url }}"
              alt=""
              aria-hidden="true"
              loading="{% if product_is_first %}eager{% else %}lazy{% endif %}"
            >

            {%- if product.images.size > 1 -%}
              <button class="all-products__arrow all-products__arrow--prev" type="button" aria-label="Previous image" data-arrow="prev" hidden>
                <svg width="14" height="14" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 11L5 7l4-4"/></svg>
              </button>
              <button class="all-products__arrow all-products__arrow--next" type="button" aria-label="Next image" data-arrow="next">
                <svg width="14" height="14" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 3l4 4-4 4"/></svg>
              </button>
            {%- endif -%}
          </div>

          <div class="all-products__info">
            <div class="all-products__head-row">
              <div class="all-products__text-group">
                <span class="all-products__title">{{ product.title }}</span>
                <span class="all-products__price">{{ product.price | money }}</span>
              </div>

              <div class="all-products__options-wrap">
                {%- unless product.has_only_default_variant -%}
                  <div class="all-products__options">
                    {%- for option in product.options_with_values -%}
                      <div class="all-products__option-group">
                        <label class="all-products__option-label" for="option-{{ product.id }}-{{ forloop.index }}">
                          {{- option.name | downcase -}}
                        </label>
                        <div class="all-products__select-wrap">
                          <select class="all-products__option-select" id="option-{{ product.id }}-{{ forloop.index }}" name="options[{{ option.name | escape }}]" data-product-id="{{ product.id }}" data-option-index="{{ forloop.index0 }}">
                            {%- for value in option.values -%}
                              <option value="{{ value | escape }}">{{ value | escape }}</option>
                            {%- endfor -%}
                          </select>
                          <svg class="all-products__select-chevron" width="10" height="10" viewBox="0 0 10 10" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M2 4l3 3 3-3"/></svg>
                        </div>
                      </div>
                    {%- endfor -%}
                  </div>
                {%- endunless -%}
              </div>
            </div>

            {%- comment -%} Rich HTML (links, paragraphs). Typography locked via .all-products__description descendant rules. {%- endcomment -%}
            <div class="all-products__description">
              {{ product.description }}
            </div>

            <button class="all-products__add-btn" type="button" aria-label="{{ 'products.product.add_to_cart' | t | default: 'Add to cart' }}" data-add-to-cart>
              <svg width="14" height="14" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" aria-hidden="true">
                <line x1="7" y1="1" x2="7" y2="13"/>
                <line x1="1" y1="7" x2="13" y2="7"/>
              </svg>
            </button>
          </div>

          <script type="application/json" data-all-products-variants>{{ product.variants | json }}</script>

        </div>
      {%- endfor -%}

      {%- if collection.products.size == 0 -%}
        <div class="all-products__empty">
          <p>No pieces yet — check back soon.</p>
        </div>
      {%- endif -%}

      {%- if settings.stockists != blank -%}
        <aside class="all-products__stockists" aria-label="{{ settings.stockists_heading | default: 'Stockists' }}">
          {%- if settings.stockists_heading != blank -%}
            <h2 class="all-products__stockists-heading">{{ settings.stockists_heading }}</h2>
          {%- endif -%}
          <div class="all-products__stockists-list">{{ settings.stockists }}</div>
        </aside>
      {%- endif -%}

    </div>
  </div>
</section>

<style>
  #shopify-section-{{ section.id }} {
    display: flex;
    flex-direction: column;
  }

  /* ── Vertical list (desktop): image left, copy right per item ── */
  .all-products__track {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    gap: clamp(4rem, 10vh, 7rem);
    box-sizing: border-box;
  }

  .all-products__card {
    width: 90%;
    max-width: 1300px;
    min-width: 0;
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: clamp(3rem, 8vw, 8rem);
    text-decoration: none;
    background: transparent;
    padding: 0;
    box-sizing: border-box;
    position: relative;
  }

  .all-products {
    width: 100%;
    height: auto;
    padding: clamp(2rem, 5vh, 3rem) clamp(1.5rem, 4vw, 3rem) clamp(2.5rem, 5vh, 3.5rem);
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    align-items: center;
    overflow: visible;
  }

  .all-products__frame {
    width: 100%;
    min-width: 0;
    height: auto;
    overflow: visible;
    position: relative;
  }

  .all-products__img-wrapper {
    flex-shrink: 0;
    width: clamp(340px, 42vw, 600px);
    aspect-ratio: 1 / 1;
    overflow: visible;
    position: relative;
  }

  .all-products__placeholder {
    position: absolute;
    inset: 0;
    background: rgba(232, 226, 210, 0.06);
    border-radius: 4px;
  }

  .all-products__img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    border-radius: 4px;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .all-products__img.is-active {
    opacity: 1;
  }

  .all-products__frame-img {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 100%;
    height: 100%;
    object-fit: contain;
    pointer-events: none;
    z-index: 5;
    display: block;
    /* Between tight crop (~1.06) and info/studio drama (~1.35) */
    transform: translate(-50%, -50%) scale(1.18);
  }

  /* ── Image gallery arrows ── */
  .all-products__arrow {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(13, 10, 7, 0.6);
    border: 1px solid rgba(232, 226, 210, 0.18);
    color: #e8e2d2;
    width: 30px;
    height: 30px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    opacity: 0;
    transition: opacity 0.2s ease, background 0.2s ease;
    z-index: 6;
    padding: 0;
  }

  .all-products__img-wrapper:hover .all-products__arrow:not([hidden]) {
    opacity: 1;
  }

  @media (hover: none) {
    .all-products__img-wrapper .all-products__arrow:not([hidden]) {
      opacity: 0.88;
    }
  }

  .all-products__arrow:hover {
    background: rgba(13, 10, 7, 0.88);
  }

  .all-products__arrow--prev { left: 0.6rem; }
  .all-products__arrow--next { right: 0.6rem; }

  /* ── Info: title/options row 1, description + add button row 2 ── */
  .all-products__info {
    flex: 1;
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, max-content);
    grid-template-rows: auto auto;
    column-gap: 1rem;
    row-gap: 0.85rem;
    align-items: start;
    align-content: start;
    max-width: 600px;
    padding: 1rem 0;
    min-width: 0;
    min-height: 0;
  }

  .all-products__head-row {
    display: contents;
  }

  .all-products__text-group {
    grid-column: 1;
    grid-row: 1;
    min-width: 0;
    display: flex;
    flex-direction: column;
    gap: 0.1rem;
  }

  .all-products__options-wrap {
    grid-column: 2;
    grid-row: 1;
    justify-self: stretch;
    text-align: right;
    min-width: 0;
    max-width: 100%;
    flex-shrink: 0;
  }

  .all-products__title {
    font-family: 'TTHoves', sans-serif;
    font-size: 1.15rem;
    font-weight: 400;
    color: #e8e2d2;
    text-transform: lowercase;
    letter-spacing: 0.02em;
  }

  .all-products__description {
    grid-column: 1 / -1;
    grid-row: 2;
    font-size: 0.88rem;
    line-height: 1.75;
    color: rgba(232, 226, 210, 0.92);
    margin: 0;
    min-width: 0;
    padding-right: calc(36px + 0.45rem);
    box-sizing: border-box;
    user-select: text;
    -webkit-user-select: text;
  }

  /* Neutralize RTE/browser defaults so markup matches the former plain-text card */
  .all-products__description :where(p, li, span, div, blockquote) {
    color: inherit;
    font-family: inherit;
    font-size: inherit;
    font-weight: inherit;
    line-height: inherit;
    letter-spacing: inherit;
  }

  .all-products__description p {
    margin: 0 0 0.65em;
  }

  .all-products__description p:last-child {
    margin-bottom: 0;
  }

  .all-products__description ul,
  .all-products__description ol {
    margin: 0 0 0.65em;
    padding-left: 1.15em;
    color: inherit;
  }

  .all-products__description strong,
  .all-products__description b {
    font-weight: 600;
    color: inherit;
  }

  .all-products__description em,
  .all-products__description i {
    font-style: italic;
    color: inherit;
  }

  /* Same cream as body copy — underline only (no default link blue) */
  .all-products__description a {
    color: inherit;
    text-decoration: underline;
    text-decoration-color: rgba(232, 226, 210, 0.35);
    text-underline-offset: 0.18em;
  }

  .all-products__description a:hover {
    text-decoration-color: rgba(232, 226, 210, 0.65);
  }

  .all-products__price {
    font-size: 0.85rem;
    letter-spacing: 0.06em;
    color: rgba(232, 226, 210, 0.72);
    margin: 0;
    text-transform: lowercase;
  }

  /* ── Option dropdowns ── */
  .all-products__options {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem 1rem;
    flex-shrink: 0;
  }

  .all-products__option-group {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .all-products__option-label {
    font-size: 0.65rem;
    letter-spacing: 0.1em;
    color: rgba(232, 226, 210, 0.72);
    text-transform: lowercase;
  }

  .all-products__select-wrap {
    position: relative;
    display: inline-flex;
    align-items: center;
  }

  .all-products__option-select {
    appearance: none;
    -webkit-appearance: none;
    background: transparent;
    border: 1px solid rgba(232, 226, 210, 0.22);
    color: #e8e2d2;
    font-family: 'TTHoves', sans-serif;
    font-size: 0.75rem;
    letter-spacing: 0.03em;
    padding: 0.3rem 1.8rem 0.3rem 0.65rem;
    cursor: pointer;
    outline: none;
    transition: border-color 0.2s ease, background 0.2s ease;
    min-width: 80px;
  }

  .all-products__option-select option {
    background: #271f16;
    color: #e8e2d2;
  }

  .all-products__option-select:hover,
  .all-products__option-select:focus {
    border-color: rgba(232, 226, 210, 0.55);
    background: rgba(232, 226, 210, 0.05);
  }

  .all-products__select-chevron {
    position: absolute;
    right: 0.5rem;
    color: rgba(232, 226, 210, 0.45);
    pointer-events: none;
    flex-shrink: 0;
  }

  .all-products__add-btn {
    grid-column: 2;
    grid-row: 2;
    align-self: start;
    justify-self: end;
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
    z-index: 1;
    background: transparent;
    border: 1px solid rgba(232, 226, 210, 0.35);
    color: #e8e2d2;
    cursor: pointer;
    transition: background 0.2s ease, border-color 0.2s ease, opacity 0.2s ease;
    flex-shrink: 0;
  }
  .all-products__add-btn:hover {
    background: rgba(232, 226, 210, 0.08);
    border-color: rgba(232, 226, 210, 0.65);
  }

  .all-products__add-btn:disabled:not([aria-busy='true']) {
    opacity: 0.35;
    cursor: not-allowed;
  }

  .all-products__add-btn:disabled:not([aria-busy='true'])::after {
    content: '';
    position: absolute;
    left: 50%;
    top: 50%;
    width: 140%;
    height: 1px;
    background: currentColor;
    transform: translate(-50%, -50%) rotate(-45deg);
    pointer-events: none;
  }

  .all-products__add-btn[aria-busy='true'] {
    opacity: 0.6;
    pointer-events: none;
  }

  .all-products__stockists {
    display: none;
  }

  @media (max-width: 900px) {
    .all-products__stockists {
      display: block;
      width: 100%;
      max-width: 100%;
      box-sizing: border-box;
      padding: clamp(0.75rem, 3vw, 1.25rem) 0 0;
      margin-top: clamp(0.5rem, 2vw, 1rem);
      border-top: 1px solid rgba(232, 226, 210, 0.12);
      color: rgba(232, 226, 210, 0.78);
      font-family: var(--font-sans);
      font-size: clamp(0.72rem, 1vw, 0.88rem);
      line-height: 1.75;
    }

    .all-products__stockists-heading {
      font-family: 'TTHoves', var(--font-sans), sans-serif;
      font-size: 0.62rem;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: #e8e2d2;
      margin: 0 0 0.6rem;
      font-weight: 400;
    }

    .all-products__stockists-list :where(p, li, span, div) {
      color: inherit;
      font-family: inherit;
      font-size: inherit;
      line-height: inherit;
      letter-spacing: inherit;
    }

    .all-products__stockists-list p {
      margin: 0 0 0.2em;
    }

    .all-products__stockists-list p:last-child {
      margin-bottom: 0;
    }

    .all-products__stockists-list a {
      color: inherit;
      text-decoration: underline;
      text-decoration-color: rgba(232, 226, 210, 0.3);
      text-underline-offset: 0.18em;
    }

    .all-products__stockists-list a:hover {
      text-decoration-color: rgba(232, 226, 210, 0.65);
    }
  }

  @media (max-width: 749px) {
    #shopify-section-{{ section.id }} {
      height: auto;
      min-height: 0;
    }

    .all-products {
      height: auto;
      min-height: calc(100dvh - 6rem);
      align-items: flex-start;
      justify-content: flex-start;
      overflow: visible;
      padding: 1rem 1.1rem calc(1.25rem + var(--maw-mobile-nav-clearance, 4rem) + env(safe-area-inset-bottom, 0px));
      box-sizing: border-box;
    }

    .all-products__frame {
      flex: none;
      width: 100%;
      height: auto;
      overflow: visible;
    }

    .all-products__track {
      gap: clamp(1.75rem, 5vw, 2.75rem);
      align-items: stretch;
    }

    .all-products__card {
      flex-direction: column;
      width: 100%;
      max-width: 100%;
      align-items: stretch;
    }

    .all-products__img-wrapper {
      width: 100%;
      margin-bottom: 1.5rem;
    }

    .all-products__info {
      flex: none;
      max-width: none;
      padding: 0;
      display: grid;
      /* Col 2 sizes to controls; description spans full width below with padding for + */
      grid-template-columns: minmax(0, 1fr) minmax(0, max-content);
      grid-template-rows: auto auto;
      column-gap: 0.55rem;
      row-gap: 0.65rem;
      align-items: start;
      align-content: start;
    }

    .all-products__head-row {
      display: contents;
    }

    .all-products__text-group {
      grid-column: 1;
      grid-row: 1;
      min-width: 0;
    }

    .all-products__options-wrap {
      grid-column: 2;
      grid-row: 1;
      justify-self: stretch;
      text-align: right;
      min-width: 0;
      max-width: 100%;
    }

    .all-products__options {
      flex-direction: row;
      flex-wrap: nowrap;
      align-items: stretch;
      justify-content: flex-end;
      gap: 0.35rem;
      width: 100%;
      min-width: 0;
    }

    .all-products__option-group {
      flex: 1 1 0;
      min-width: 0;
      align-items: stretch;
    }

    .all-products__option-group:only-child {
      flex: 0 0 auto;
      max-width: 100%;
      align-self: flex-end;
    }

    .all-products__option-group:only-child .all-products__select-wrap {
      width: auto;
      max-width: min(7rem, 52vw);
    }

    .all-products__option-group:only-child .all-products__option-select {
      width: auto;
      min-width: 3.85rem;
      max-width: 100%;
    }

    .all-products__option-label {
      font-size: 0.52rem;
      letter-spacing: 0.06em;
      line-height: 1.2;
    }

    .all-products__select-wrap {
      width: 100%;
      max-width: 100%;
    }

    .all-products__option-select {
      font-size: 0.56rem;
      letter-spacing: 0.01em;
      padding: 0.18rem 1.05rem 0.18rem 0.35rem;
      min-width: 0;
      width: 100%;
      max-width: 100%;
      box-sizing: border-box;
    }

    .all-products__select-chevron {
      width: 8px;
      height: 8px;
      right: 0.3rem;
    }

    .all-products__description {
      grid-column: 1 / -1;
      grid-row: 2;
      min-width: 0;
      /* Full-width copy; reserve only the + control (no whole option-column gutter) */
      padding-right: calc(36px + 0.45rem);
      box-sizing: border-box;
    }

    .all-products__add-btn {
      grid-column: 2;
      grid-row: 2;
      align-self: start;
      justify-self: end;
      margin-top: 0;
      position: relative;
      z-index: 1;
    }

    /* Photo sits slightly inside the decorative frame (tape border visible as mat) */
    .all-products__img {
      top: 6%;
      left: 6%;
      width: 88%;
      height: 88%;
    }

    .all-products__placeholder {
      inset: auto;
      top: 6%;
      left: 6%;
      width: 88%;
      height: 88%;
    }

    .all-products__frame-img {
      transform: translate(-50%, -50%) scale(1.1);
    }

    .all-products__arrow {
      display: none !important;
    }
  }
</style>

<script>
  (function () {
    const track = document.getElementById('all-products-track');
    if (!track) return;

    const swipeMQ = window.matchMedia('(max-width: 749px)');
    const SWIPE_MIN = 45;

    /* ── Image gallery cycling (+ swipe on mobile, arrows hidden) ── */
    track.querySelectorAll('.all-products__img-wrapper').forEach((wrapper) => {
      const imgs    = wrapper.querySelectorAll('.all-products__img');
      const prevBtn = wrapper.querySelector('[data-arrow="prev"]');
      const nextBtn = wrapper.querySelector('[data-arrow="next"]');
      if (imgs.length <= 1) return;

      let current = 0;

      function goTo(index) {
        imgs[current].classList.remove('is-active');
        current = index;
        imgs[current].classList.add('is-active');
        if (prevBtn) prevBtn.hidden = current === 0;
        if (nextBtn) nextBtn.hidden = current === imgs.length - 1;
      }

      function goPrev() {
        if (current > 0) goTo(current - 1);
      }

      function goNext() {
        if (current < imgs.length - 1) goTo(current + 1);
      }

      prevBtn && prevBtn.addEventListener('click', (e) => {
        e.stopPropagation();
        goPrev();
      });

      nextBtn && nextBtn.addEventListener('click', (e) => {
        e.stopPropagation();
        goNext();
      });

      let swipeTracking = false;
      let swipeStartX = 0;
      let swipeStartY = 0;

      wrapper.addEventListener('touchstart', (e) => {
        if (!swipeMQ.matches || e.touches.length !== 1) return;
        swipeTracking = true;
        swipeStartX = e.touches[0].clientX;
        swipeStartY = e.touches[0].clientY;
      }, { passive: true });

      wrapper.addEventListener('touchmove', (e) => {
        if (!swipeTracking || !swipeMQ.matches) return;
        const t = e.touches[0];
        const dx = t.clientX - swipeStartX;
        const dy = t.clientY - swipeStartY;
        if (Math.abs(dy) > Math.abs(dx) && Math.abs(dy) > 12) swipeTracking = false;
      }, { passive: true });

      wrapper.addEventListener('touchend', (e) => {
        if (!swipeTracking || !swipeMQ.matches) {
          swipeTracking = false;
          return;
        }
        swipeTracking = false;
        const t = e.changedTouches[0];
        const dx = t.clientX - swipeStartX;
        const dy = t.clientY - swipeStartY;
        if (Math.abs(dx) < SWIPE_MIN || Math.abs(dx) < Math.abs(dy)) return;
        if (dx > 0) goPrev();
        else if (dx < 0) goNext();
      }, { passive: true });

      wrapper.addEventListener('touchcancel', () => {
        swipeTracking = false;
      }, { passive: true });
    });

    function getCardVariants(card) {
      const el = card.querySelector('script[type="application/json"][data-all-products-variants]');
      if (!el) return [];
      try {
        return JSON.parse(el.textContent);
      } catch (err) {
        return [];
      }
    }

    function getSelectedVariant(card) {
      const variants = getCardVariants(card);
      if (!variants.length) return null;
      const selects = card.querySelectorAll('.all-products__option-select');
      if (!selects.length) {
        return variants.find((v) => v.available) || variants[0];
      }
      const selected = Array.from(selects).map((s) => s.value);
      return variants.find(
        (v) =>
          v.options.length === selected.length &&
          v.options.every((opt, i) => opt === selected[i]),
      ) || null;
    }

    function syncAddButton(card) {
      const btn = card.querySelector('[data-add-to-cart]');
      if (!btn) return;
      const def = btn.getAttribute('data-default-label') || 'Add to cart';
      const v = getSelectedVariant(card);
      const ok = v && v.available;
      btn.disabled = !ok;
      if (!v) {
        btn.setAttribute('aria-label', def);
      } else if (!ok) {
        btn.setAttribute('aria-label', 'Sold out');
      } else {
        btn.setAttribute('aria-label', def);
      }
    }

    track.querySelectorAll('.all-products__card').forEach((card) => {
      const btn = card.querySelector('[data-add-to-cart]');
      if (btn && !btn.getAttribute('data-default-label')) {
        btn.setAttribute('data-default-label', btn.getAttribute('aria-label') || 'Add to cart');
      }
      syncAddButton(card);
      card.querySelectorAll('.all-products__option-select').forEach((sel) => {
        sel.addEventListener('change', () => syncAddButton(card));
      });
    });

    track.addEventListener('click', async (e) => {
      const btn = e.target.closest('[data-add-to-cart]');
      if (!btn || !track.contains(btn)) return;
      e.preventDefault();
      const card = btn.closest('.all-products__card');
      if (!card || btn.disabled) return;
      const v = getSelectedVariant(card);
      if (!v || !v.available) return;
      if (!window.MAWCart || typeof window.MAWCart.add !== 'function') return;

      const defaultLabel = btn.getAttribute('data-default-label') || 'Add to cart';
      btn.setAttribute('aria-busy', 'true');
      btn.disabled = true;
      let added = false;
      try {
        await window.MAWCart.add({ id: v.id, quantity: 1 });
        added = true;
      } catch (err) {
        console.error(err);
        btn.setAttribute('aria-label', 'Could not add — try again');
        setTimeout(() => {
          btn.setAttribute('aria-label', defaultLabel);
          syncAddButton(card);
        }, 2200);
      } finally {
        btn.removeAttribute('aria-busy');
      }
      if (added) syncAddButton(card);
    });
  })();
</script>

{% schema %}
{
  "name": "All products scroll",
  "settings": [],
  "presets": [
    { "name": "All products scroll" }
  ]
}
{% endschema %}
